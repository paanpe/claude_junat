name: Julkaise GitHub Pages -sivustolle

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Hae koodi
        uses: actions/checkout@v4

      - name: Määritä Pages
        uses: actions/configure-pages@v5

      - name: Pakkaa julkaistava sisältö
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: Julkaise Pages-sivustolle
        id: deployment
        uses: actions/deploy-pages@v4
